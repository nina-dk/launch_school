console.log('5' + 10) // '510'

//This is the result of JavaScript's implicit type coercion, which coerces
//the number 10 into a string. Then it concatenates the strings '5' + '10',
//thus printing '510' to the console. This happens when either operand of
//the addition is a String.