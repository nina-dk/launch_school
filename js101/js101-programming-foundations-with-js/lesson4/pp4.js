['ant', 'bear', 'caterpillar'].pop().length

//11
//The `pop` method removes the last element from an array and returns
//that element. In the case above, the string 'caterpillar' will be removed and
//returned by `pop`. Then `length` is called on that string, so 11 is returned.